eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8075
spring:
  application:
    name: examination

  data:
    mongodb:
      database: ms2023
      host: localhost
      port: 27017
